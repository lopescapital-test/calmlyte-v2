{
  "name": "calmlyte-site",
  "version": "0.0.0",
  "private": true,
  "description": "Static build pipeline for the Calmlyte site. Generates build/ from the approved artboards in 'Calmlyte Approved Site/'.",
  "scripts": {
    "build": "node tools/build-site.js",
    "verify": "node tools/verify-build.js"
  },
  "engines": {
    "node": "22.x"
  }
}
